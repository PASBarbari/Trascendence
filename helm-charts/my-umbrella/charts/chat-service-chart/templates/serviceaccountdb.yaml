apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.dbName | default "chat-db-service-account" }}
  namespace: {{ .Values.namespace | default "chat" }}
  labels:
    app: {{ .Release.Name }}-chat-db
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
  {{- with .Values.serviceAccount.dbAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.serviceAccount.dbAutomount }}