apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-chat-configmap
  namespace: {{ .Values.namespace }}
data:
  {{- range $key, $val := .Values.configMap.data }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}